/* ─────────────────────────────────────────────────
   ARORA SWEETS — FIREBASE CONFIG
   Fill in both values after creating your Firebase
   Realtime Database (see admin.html for setup guide)
   ───────────────────────────────────────────────── */
var ARORA_FB = {
  // Your Realtime Database URL
  // Example: 'https://arora-sweets-default-rtdb.firebaseio.com'
  dbUrl: 'https://arorasweets-35a7b-default-rtdb.firebaseio.com/',

  // Your Firebase Web API Key — this is SAFE to be public; it only
  // identifies the project and grants nothing by itself. Real access
  // control lives in Realtime Database → Rules and in the owner's
  // Firebase Authentication login (see js/owner-auth.js).
  // Firebase Console → Project Settings → General → Web API Key
  apiKey: 'AIzaSyDEZeOJfNjIfH-ZtX4S7zdarfJOLpumsW0'
};
